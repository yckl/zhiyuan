# 校园志愿者系统答辩问答总结（基于当前项目与PPT）

## 1. 项目技术栈与“为什么用”

### 1.1 Spring Boot
- 用途：承载后端业务接口与分层架构（Controller/Service/Mapper）。
- 为什么用：开发效率高、生态成熟，适合毕业设计多模块快速落地。
- 在本项目中的实现：登录、活动、报名、签到、审核、工时、积分等接口均在 Spring Boot 中实现。

### 1.2 Spring Security + JWT
- 用途：认证与权限控制（VOLUNTEER/ORGANIZER/ADMIN）。
- 为什么用：前后端分离项目中，JWT 无状态认证更适配；Security 便于角色权限边界控制。
- 在本项目中的实现：
  - 登录签发 JWT（24 小时有效）。
  - 请求过滤器解析 JWT、注入认证上下文。
  - 接口权限与角色路由双重控制（后端拦接口，前端拦页面）。

### 1.3 MyBatis-Plus + MySQL
- 用途：核心业务数据存储与查询（用户、活动、报名、签到、积分）。
- 为什么用：结构化数据强一致；MyBatis-Plus 简化 CRUD，同时支持关键 SQL 自定义。
- 在本项目中的实现：并发报名采用条件更新 SQL，保证名额一致性。

### 1.4 Redis
- 用途：短时安全状态缓存。
- 为什么用：读写快、支持 TTL，适合临时风控和令牌状态。
- 在本项目中的实现：
  - 登录失败限流计数（防暴力破解）。
  - JWT 黑名单（退出登录后让 token 立即失效）。

### 1.5 Vue3 + TypeScript + Pinia + Vue Router + Element Plus
- 用途：前端多角色、多终端页面实现。
- 为什么用：组件化与类型约束提升可维护性；Router/Pinia 支持复杂状态与权限流转。
- 在本项目中的实现：志愿者端、组织者端、管理员端同仓实现，移动端扫码与 PC 管理协同。

### 1.6 html5-qrcode
- 用途：手机端扫码签到。
- 为什么用：Web 端可直接调用摄像头，快速实现扫码闭环。

### 1.7 JMeter
- 用途：并发报名场景压测。
- 为什么用：可模拟多账号并发，验证“是否超卖”和接口稳定性。

---

## 2. 核心功能实现逻辑（PPT重点）

## 2.1 报名并发名额控制（第10页）

### 业务目标
多人同时点报名时，不能超出活动名额。

### 实现逻辑链
1. 前端提交 `activityId` + JWT。
2. 后端先做规则校验：
   - 活动状态是否可报名
   - 报名时间是否有效
   - 是否重复报名
   - 是否存在时间冲突
3. 执行数据库原子更新（关键）：
   - `current_participants = current_participants + 1`
   - 条件：`current_participants < max_participants`
4. 通过影响行数判断结果：
   - 影响行数 = 1：抢位成功
   - 影响行数 = 0：名额已满
5. 抢位成功后再写报名记录；失败直接返回“名额已满”。

### 关键点
- 不采用“先查剩余再更新”。
- 判断与更新放在同一条 SQL，避免并发竞争窗口。

---

## 2.2 动态二维码签到（第11页）

### 业务目标
减少截图转发代签、重复签到、非名单签到。

### 实现逻辑链
1. 组织者端生成动态 token：`activityId:expireAt:nonce`。
2. token 短时有效（当前实现 30 秒），并编码为二维码内容。
3. 志愿者扫码后，前端提交 token 到服务端验证接口。
4. 服务端统一校验：
   - 时效锁：token 是否过期
   - 活动锁：token 对应活动是否合法
   - 身份锁：当前用户是否在该活动有效报名名单
   - 状态锁：是否重复签到、是否具备签到资格
5. 全部通过后才更新：
   - `signInTime`
   - 报名状态改为“已签到”

### 关键点
- 前端只负责采集与交互，最终以服务端校验为准。
- 短时动态码 + 多重校验形成防作弊闭环。

---

## 2.3 推荐排序（第12页）

### 业务目标
提升活动推荐相关性，同时优先曝光紧迫活动。

### 实现逻辑链
1. 候选筛选：仅保留可参与活动（审核通过、未截止、状态可参与）。
2. 多因子加权打分：
   - 历史参与偏好（常参与分类）
   - 兴趣标签匹配
   - 截止紧迫度
   - 名额紧张度
   - 探索发现（适度多样性）
3. 按总分排序输出 TopN。
4. 同时返回推荐标签与理由（可解释展示）。

### 关键点
- 不是单纯按时间排序。
- 推荐有理由，具备可解释性。

---

## 2.4 多角色 + 手机/PC双端（第13-14页）

### 业务目标
同一系统支撑“现场移动执行 + 后台桌面管理”。

### 实现逻辑
- 志愿者端：活动浏览、报名、扫码签到。
- 组织者端：发布活动、审核报名、现场核销、工时录入。
- 管理员端：审核与运营管理。
- 路由守卫按角色控制页面访问，后端权限控制接口访问。
- 通过响应式与布局策略适配移动端与 PC。

### 关键点
- 不是简单缩放页面，而是按角色与终端重构交互路径。

---

## 3. Redis相关问答总结

## 3.1 登录失败限流是什么意思？
- 含义：同一个“用户名 + IP”在短时间内连续输错密码超过阈值，会被临时限制登录尝试。
- 本项目实现：
  - key：`login:limit:用户名:IP`
  - value：失败次数
  - 阈值：5 次
  - TTL：15 分钟

## 3.2 Redis缓存了什么数据？
- 登录失败次数计数（限流 key）。
- JWT 黑名单标记（`jwt:blacklist:token`，值一般为 `logout`）。

## 3.3 JWT黑名单是干什么的？
- 作用：让“还没自然过期的 token”在退出后立即失效。
- 流程：
  1. 退出登录时把 token 放入黑名单并设置 TTL（剩余有效期）。
  2. 请求过滤器每次先查黑名单，命中则拒绝认证。

## 3.4 Redis过期时间是多少？
- 登录失败限流 key：15 分钟。
- JWT黑名单 key：按 token 剩余有效期动态设置（毫秒）。

---

## 4. JWT有效期
- 当前项目 JWT 有效期：`86400000 ms = 24 小时`。

---

## 5. 压力测试脚本思路（JMeter）

### 目标场景
- 60个账号并发报名同一活动（名额30），验证不超卖。

### 脚本结构
1. 线程组：`Threads=60`，`Ramp-Up=1~3s`，`Loop=1`。
2. CSV Data Set：60组账号密码。
3. HTTP请求A：`POST /auth/login` 获取 token。
4. JSON提取器：提取 `$.data.token`。
5. HTTP请求B：`POST /registration`，Header 带 `Bearer token`。
6. 断言：
   - “报名成功” 或
   - “名额已满”（预期业务拒绝）
7. 最终核验：活动当前人数与有效报名记录一致。

### 结果解释
- “30成功 + 30满员拒绝”是正确业务结果，不是系统异常。

---

## 6. 三大创新点（可直接用于答辩总结）

1. 并发报名防超卖的数据库原子策略。
2. 动态二维码签到的多锁校验防作弊闭环。
3. 推荐结果可解释与业务紧迫性结合。

这些点都超出了普通 CRUD 展示，体现了并发一致性、安全真实性和业务智能化能力。

---

## 7. 老师可能追问的问题（高频）

1. 为什么用数据库条件更新，不用 `synchronized`？
- 答：`synchronized` 只在单JVM有效，数据库原子更新适用于多实例并发，工程上更稳。

2. 更新名额成功但报名记录写失败怎么办？
- 答：流程在事务中，异常会回滚，保证一致性。

3. 二维码为什么要短时有效？
- 答：压缩截图传播窗口，降低代签风险；时长可配置。

4. Base64不是加密，安全吗？
- 答：安全性依赖服务端校验链（时效/身份/活动/状态），不是依赖前端隐藏；后续可升级签名token。

5. 推荐算法效果如何评估？
- 答：可用点击率、报名转化率、活动完成率等指标做A/B优化。

6. 为什么失败30次不算系统失败？
- 答：那是“名额已满”的预期业务拒绝，不是异常或错误码500。

7. 手机PC双端算创新吗？
- 答：算。因为是一套系统同时支持移动端现场执行与PC端后台管理，并按角色重构流程，不是简单响应式缩放。

---

## 8. 最终高分收束话术（建议结尾使用）
本课题的核心贡献不是功能堆叠，而是把校园志愿活动中的高风险环节做成了可验证的工程闭环：
- 报名环节通过数据库原子条件更新保证并发一致性；
- 签到环节通过动态二维码与服务端多锁校验保证真实性；
- 推荐环节通过可解释加权排序提升活动匹配效率。
同时通过功能测试与并发测试证明结果与业务预期一致，形成了“发布-审核-报名-签到-工时-激励”的连续可追溯链路。
